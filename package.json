{
  "version": "0.1.0",
  "scripts": {
    "build": "esbuild index.js --bundle --minify --format=esm --target=chrome100 --outdir=dist"
  },
  "devDependencies": {
    "esbuild": "^0.18.14"
  },
  "dependencies": {
    "socket.io-client": "2.5.0"
  }
}
